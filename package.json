{
  "name": "kitten-customizer",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/worker"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "dev:worker": "npm --workspace apps/worker run dev",
    "deploy:worker": "npm --workspace apps/worker run deploy"
  }
}
